# Secrets — never commit
.env
.demo_service_role_key
*.service_role_key

# Heavy data — embedding_3d.json IS intentionally tracked for Vercel deploy (13 MB)
# (keep other heavy locals ignored here if needed)

# OS / editor
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Vercel / node
node_modules/
.vercel

.vercel

site*.log
